## Agent skills

### Issue tracker

工作項目發布至 `PICDarcy/MULTI_PROTOCOL_PLC_HMI` 的 GitHub Issues；外部 Pull Request 不納入任務分流。See `docs/agents/issue-tracker.md`.

### Triage labels

使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix` 五個分流標籤。See `docs/agents/triage-labels.md`.

### Domain docs

本專案採 single-context 配置，使用根目錄 `CONTEXT.md` 與 `docs/adr/`。See `docs/agents/domain.md`.
